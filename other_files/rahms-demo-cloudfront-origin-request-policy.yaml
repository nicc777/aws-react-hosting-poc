OriginRequestPolicyConfig:  # [REQUIRED] An origin request policy configuration.
  Comment: 'demo origin policy'  # A comment to describe the origin request policy.
  Name: 'RahmsDemoOriginPolicy' # [REQUIRED] A unique name to identify the origin request policy.
  HeadersConfig: # [REQUIRED] The HTTP headers to include in origin requests.
    HeaderBehavior: allViewer  # [REQUIRED] Determines whether any HTTP headers are included in requests that CloudFront sends to the origin. Valid values are: none, whitelist, allViewer, allViewerAndWhitelistCloudFront.
    # Headers: # Contains a list of HTTP header names.
    #   Quantity: 0  # [REQUIRED] The number of header names in the Items list.
    #   Items: # A list of HTTP header names.
    #   - ''
  CookiesConfig: # [REQUIRED] The cookies from viewer requests to include in origin requests.
    CookieBehavior: all  # [REQUIRED] Determines whether cookies in viewer requests are included in requests that CloudFront sends to the origin. Valid values are: none, whitelist, all.
    # Cookies: # Contains a list of cookie names.
    #   Quantity: 0  # [REQUIRED] The number of cookie names in the Items list.
    #   Items: # A list of cookie names.
    #   - ''
  QueryStringsConfig: # [REQUIRED] The URL query strings from viewer requests to include in origin requests.
    QueryStringBehavior: all  # [REQUIRED] Determines whether any URL query strings in viewer requests are included in requests that CloudFront sends to the origin. Valid values are: none, whitelist, all.
    # QueryStrings: # Contains a list of the query strings in viewer requests that are included in requests that CloudFront sends to the origin.
    #   Quantity: 0  # [REQUIRED] The number of query string names in the Items list.
    #   Items: # A list of query string names.
    #   - ''
